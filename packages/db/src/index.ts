// @nova/db — single source of truth for the Prisma client, model types, and enums.
// The NestJS PrismaService (apps/api) extends PrismaClient exported here.
export * from '@prisma/client';
export { PrismaClient, Prisma } from '@prisma/client';
